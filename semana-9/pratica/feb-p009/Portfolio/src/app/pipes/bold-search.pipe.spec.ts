import { BoldSearchPipe } from './bold-search.pipe';

describe('BoldSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new BoldSearchPipe();
    expect(pipe).toBeTruthy();
  });
});

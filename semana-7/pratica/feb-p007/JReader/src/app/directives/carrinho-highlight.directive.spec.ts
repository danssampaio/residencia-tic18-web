import { CarrinhoHighlightDirective } from './carrinho-highlight.directive';

describe('CarrinhoHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new CarrinhoHighlightDirective();
    expect(directive).toBeTruthy();
  });
});

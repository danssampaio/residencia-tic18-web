import { ClassHighlightDirective } from './class-highlight.directive';

describe('ClassHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new ClassHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
